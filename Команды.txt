git -v/git -- version
git config --global user.name 'name'
git config --global user.email 'email'
ssh-keygen -t ed25519 -C 'email'
git bash
eval "$(ssh-agent -s)" - запускает агента
ssh-add C:/Users/User/.ssh/id_ed25519
clip < C:/Users/User/.ssh/id_ed25519.pub
mkdir test
cd test
git clone git@github.com:SoulEnot/Test

git branch - показывает ветки
git status - показывает стаутс ветки
git add -A(весь проект commit)
git add file name

git commit -m 'massage' - фиксируем изменения на локальной версии
git chechout -b name_branch - создание ветки
echo строка >> text.txt - создание файла
git checkout main - Переключение на ветку Имя ветки
git merge new_branch - слияние из main версии с новой веткой
git push origin new_branch

:q - выход из синих букв